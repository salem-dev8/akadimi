<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>إدارة المواد | أكاديمية المعالي</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>@import url('https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap'); body { font-family: 'Cairo', sans-serif; background: #f8fafc; }</style>
</head>
<body class="flex">
    <aside class="w-64 bg-slate-900 text-white min-h-screen p-6">
        <div class="text-2xl font-bold text-blue-400 mb-10 text-center">أكاديمية المعالي</div>
        <nav class="space-y-4">
            <a href="/" class="block p-3 hover:bg-slate-800 rounded-xl"><i class="fas fa-chart-pie ml-2"></i> الإحصائيات</a>
            <a href="/students" class="block p-3 hover:bg-slate-800 rounded-xl"><i class="fas fa-users ml-2"></i> الطلاب</a>
            <a href="/subjects" class="block p-3 bg-blue-600 rounded-xl"><i class="fas fa-book ml-2"></i> المواد</a>
            <a href="/sessions" class="block p-3 hover:bg-slate-800 rounded-xl"><i class="fas fa-clock ml-2"></i> الحصص</a>
        </nav>
    </aside>

    <main class="flex-1 p-8">
        <div class="flex justify-between items-center mb-10">
            <h1 class="text-3xl font-bold text-slate-800">قائمة المواد والأسعار</h1>
            <button onclick="toggleModal()" class="bg-indigo-600 text-white px-6 py-2 rounded-xl">+ إضافة مادة جديدة</button>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            <% subjects.forEach(sub => { %>
            <div class="bg-white p-6 rounded-3xl shadow-sm border border-slate-100 hover:shadow-md transition">
                <div class="flex justify-between items-start mb-4">
                    <div class="w-12 h-12 bg-blue-50 text-blue-600 rounded-2xl flex items-center justify-center text-xl">
                        <i class="fas <%= sub.icon || 'fa-book' %>"></i>
                    </div>
                    <span class="bg-green-100 text-green-700 px-3 py-1 rounded-full text-xs font-bold"><%= sub.price %> دج</span>
                </div>
                <h3 class="text-xl font-bold mb-1"><%= sub.name %></h3>
                <p class="text-slate-500 text-sm"><%= sub.phase %></p>
            </div>
            <% }) %>
        </div>

        <div id="subModal" class="hidden fixed inset-0 bg-slate-900/50 backdrop-blur-sm flex items-center justify-center p-4">
            <form action="/subjects/add" method="POST" class="bg-white w-full max-w-md p-8 rounded-3xl shadow-2xl">
                <h2 class="text-2xl font-bold mb-6">إضافة مادة للمنهاج</h2>
                <div class="space-y-4">
                    <input name="name" placeholder="اسم المادة" class="w-full border p-4 rounded-2xl outline-none focus:ring-2 ring-blue-500" required>
                    <select name="phase" class="w-full border p-4 rounded-2xl outline-none">
                        <option>ابتدائي</option>
                        <option>متوسط</option>
                        <option>ثانوي (جذع مشترك)</option>
                        <option>ثانوي (علوم تجريبية)</option>
                        <option>ثانوي (تسيير واقتصاد)</option>
                        <option>ثانوي (تقني رياضي)</option>
                    </select>
                    <input name="price" type="number" placeholder="سعر الحصة (دج)" class="w-full border p-4 rounded-2xl outline-none" required>
                    <select name="icon" class="w-full border p-4 rounded-2xl outline-none">
                        <option value="fa-calculator">رياضيات</option>
                        <option value="fa-microscope">علوم / فيزياء</option>
                        <option value="fa-language">لغات</option>
                        <option value="fa-gavel">قانون / تسيير</option>
                        <option value="fa-pen-nib">أدب / فلسفة</option>
                    </select>
                </div>
                <div class="mt-8 flex gap-3">
                    <button type="submit" class="flex-1 bg-blue-600 text-white py-4 rounded-2xl font-bold">حفظ المادة</button>
                    <button type="button" onclick="toggleModal()" class="flex-1 bg-slate-100 py-4 rounded-2xl">إلغاء</button>
                </div>
            </form>
        </div>
    </main>
    <script>function toggleModal() { document.getElementById('subModal').classList.toggle('hidden'); }</script>
</body>
</html>
