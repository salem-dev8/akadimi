<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>أكاديمية المعالي PRO</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;900&display=swap');
        body { font-family: 'Cairo', sans-serif; background: #f8fafc; }
        .view-section { display: none; }
        .view-section.active { display: block; animation: fadeIn 0.3s; }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
        .compact-stat { padding: 0.75rem; border-radius: 1rem; }
        .active-dot { background: #10b981 !important; color: white !important; border-color: #10b981 !important; }
    </style>
</head>
<body class="pt-16">

    <nav class="fixed top-0 left-0 right-0 bg-white/90 backdrop-blur-md border-b h-14 flex items-center justify-between px-4 z-[1000]">
        <div class="flex items-center gap-2">
            <button onclick="toggleSidebar()" class="p-2 text-slate-600"><i class="fas fa-bars"></i></button>
            <img src="https://res.cloudinary.com/duixjs8az/image/upload/v1765132270/post_media/1765132269901-1765132158292.jpg" class="w-8 h-8 rounded-full">
            <span class="text-xs font-900 text-slate-800">أكاديمية المعالي</span>
        </div>
        <button onclick="openModal()" class="bg-blue-600 text-white px-3 py-1.5 rounded-lg text-[10px] font-bold">
            <i class="fas fa-plus"></i> تسجيل
        </button>
    </nav>

    <aside id="sidebar" class="fixed inset-y-0 right-0 w-64 bg-slate-900 text-white p-6 transform translate-x-full transition-transform z-[1001]">
        <div class="flex justify-between mb-8"><span class="font-bold">القائمة</span><button onclick="toggleSidebar()"><i class="fas fa-times"></i></button></div>
        <div class="space-y-2 text-sm">
            <button onclick="showView('statsView')" class="w-full text-right p-3 hover:bg-white/10 rounded-xl flex items-center gap-3"><i class="fas fa-chart-pie"></i> الإحصائيات</button>
            <button onclick="showView('attendanceView')" class="w-full text-right p-3 hover:bg-white/10 rounded-xl flex items-center gap-3"><i class="fas fa-tasks"></i> الحصص</button>
            <button onclick="showView('studentsView')" class="w-full text-right p-3 hover:bg-white/10 rounded-xl flex items-center gap-3"><i class="fas fa-users"></i> الطلاب</button>
        </div>
    </aside>

    <main class="p-3 max-w-2xl mx-auto">
        
        <section id="statsView" class="view-section active space-y-3">
            <div class="grid grid-cols-2 gap-2">
                <div class="bg-white compact-stat shadow-sm border-r-4 border-blue-500">
                    <p class="text-[9px] text-slate-400 font-bold uppercase">الطلاب</p>
                    <h3 class="text-lg font-900"><%= stats.total %></h3>
                </div>
                <div class="bg-white compact-stat shadow-sm border-r-4 border-green-500">
                    <p class="text-[9px] text-slate-400 font-bold uppercase">المدفوعات</p>
                    <h3 class="text-lg font-900 text-green-600"><%= stats.paid %></h3>
                </div>
            </div>
            
            <div class="bg-white p-3 rounded-2xl shadow-sm">
                <p class="text-[10px] font-bold mb-2 opacity-50">توزيع المستويات</p>
                <div class="h-40"><canvas id="pieChart"></canvas></div>
            </div>
            
        </section>

        <section id="attendanceView" class="view-section space-y-2">
            <% students.forEach(s => { %>
            <div class="bg-white p-3 rounded-xl shadow-sm border border-slate-50">
                <div class="flex justify-between items-center mb-2">
                    <h4 class="text-xs font-bold truncate w-32"><%= s.name %></h4>
                    <button onclick="updateAjax('<%= s.id %>', 'paid', null, this)" 
                        class="text-[9px] px-2 py-1 rounded-md font-bold <%= s.paid ? 'bg-green-100 text-green-700' : 'bg-red-50 text-red-600' %>">
                        <%= s.paid ? 'خالص' : 'غير خالص' %>
                    </button>
                </div>
                <div class="flex justify-between items-center">
                    <div class="flex gap-1.5">
                        <% for(let i=0; i<4; i++) { %>
                        <button onclick="updateAjax('<%= s.id %>', 'attendance', <%= i %>, this)" 
                            class="w-7 h-7 rounded-md border text-[9px] font-bold transition-all <%= s.attendance && s.attendance[i] ? 'active-dot' : 'bg-slate-50 text-slate-300' %>">
                            <%= i+1 %>
                        </button>
                        <% } %>
                    </div>
                    <span class="text-[9px] text-blue-500 font-bold"><%= s.subject %></span>
                </div>
            </div>
            <% }) %>
        </section>

        <section id="studentsView" class="view-section space-y-2">
            <% students.forEach(s => { %>
            <div class="bg-white p-3 rounded-xl flex items-center justify-between shadow-sm border border-slate-50">
                <div class="flex items-center gap-3">
                    <div class="w-8 h-8 bg-slate-100 rounded-lg flex items-center justify-center text-xs font-bold text-blue-600"><%= s.name[0] %></div>
                    <div>
                        <p class="text-xs font-bold"><%= s.name %></p>
                        <p class="text-[9px] text-slate-400"><%= s.cycle %> • <%= s.stream || 'عام' %></p>
                    </div>
                </div>
                <form action="/delete/<%= s.id %>" method="POST" onsubmit="return confirm('حذف الطالب نهائياً؟')">
                    <button type="submit" class="text-slate-300 hover:text-red-500 p-2"><i class="fas fa-trash-alt text-xs"></i></button>
                </form>
            </div>
            <% }) %>
        </section>
    </main>

    <div id="addModal" class="fixed inset-0 bg-slate-900/60 hidden backdrop-blur-sm z-[2000] flex items-end md:items-center justify-center">
        <form action="/add-student" method="POST" class="bg-white w-full md:max-w-md p-6 rounded-t-3xl md:rounded-3xl shadow-2xl space-y-4">
            <h2 class="text-lg font-900 text-slate-800">تسجيل طالب جديد</h2>
            
            <input name="name" type="text" placeholder="الاسم الكامل" class="w-full bg-slate-50 p-3 rounded-xl text-sm outline-none border border-slate-100" required>
            
            <div class="grid grid-cols-2 gap-3">
                <select id="cycleSel" name="cycle" onchange="handleCycleChange()" class="bg-slate-50 p-3 rounded-xl text-sm outline-none border-none" required>
                    <option value="">اختر الطور</option>
                    <option>ابتدائي</option><option>متوسط</option><option>ثانوي</option>
                </select>
                <select id="yearSel" name="year" class="bg-slate-50 p-3 rounded-xl text-sm outline-none border-none" required>
                    <option value="">السنة</option>
                </select>
            </div>

            <select id="streamSel" name="stream" onchange="handleStreamChange()" class="w-full bg-slate-50 p-3 rounded-xl text-sm hidden outline-none border-none">
                <option value="">الشعبة</option>
                <option>علوم تجريبية</option><option>رياضيات</option><option>تقني رياضي</option>
                <option>تسيير واقتصاد</option><option>آداب وفلسفة</option><option>لغات أجنبية</option>
            </select>

            <select id="subjectSel" name="subject" class="w-full bg-slate-50 p-3 rounded-xl text-sm outline-none border-none" required>
                <option value="">المادة</option>
            </select>

            <div class="flex gap-2 pt-4">
                <button type="submit" class="flex-1 bg-blue-600 text-white py-3 rounded-xl font-bold text-sm">حفظ البيانات</button>
                <button type="button" onclick="closeModal()" class="flex-1 bg-slate-100 text-slate-500 py-3 rounded-xl font-bold text-sm">إلغاء</button>
            </div>
        </form>
    </div>

    <script>
        // 12 مادة أساسية بالإضافة لمواد الشعب
        const subjectsData = {
            "ابتدائي": ["لغة عربية", "رياضيات", "فرنسية", "إنجليزية", "تربية إسلامية"],
            "متوسط": ["رياضيات", "فيزياء", "علوم طبيعية", "لغة عربية", "فرنسية", "إنجليزية", "تاريخ وجغرافيا", "تربية مدنية"],
            "عام": ["رياضيات", "فيزياء", "علوم", "عربية", "فرنسية", "إنجليزية", "تاريخ", "فلسفة", "إسلامية", "اعلام آلي", "رسم", "رياضة"],
            "علوم تجريبية": ["علوم طبيعية (أساسية)", "فيزياء", "رياضيات"],
            "رياضيات": ["رياضيات (أساسية)", "فيزياء"],
            "تقني رياضي": ["تكنولوجيا (هندسة)", "رياضيات", "فيزياء"],
            "تسيير واقتصاد": ["محاسبة", "اقتصاد", "قانون"],
            "آداب وفلسفة": ["فلسفة (أساسية)", "لغة عربية", "جغرافيا"],
            "لغات أجنبية": ["لغة ثالثة", "إسبانية", "ألمانية", "إيطالية"]
        };

        function handleCycleChange() {
            const cycle = document.getElementById('cycleSel').value;
            const yearSel = document.getElementById('yearSel');
            const streamSel = document.getElementById('streamSel');
            const subSel = document.getElementById('subjectSel');

            yearSel.innerHTML = '<option value="">السنة</option>';
            const years = cycle === "ابتدائي" ? [1,2,3,4,5] : cycle === "متوسط" ? [1,2,3,4] : [1,2,3];
            years.forEach(y => yearSel.innerHTML += `<option>السنة ${y}</option>`);

            streamSel.classList.toggle('hidden', cycle !== 'ثانوي');
            
            subSel.innerHTML = '<option value="">المادة</option>';
            const baseSubjects = subjectsData[cycle] || subjectsData["عام"];
            baseSubjects.forEach(s => subSel.innerHTML += `<option>${s}</option>`);
        }

        function handleStreamChange() {
            const stream = document.getElementById('streamSel').value;
            const subSel = document.getElementById('subjectSel');
            const cycle = document.getElementById('cycleSel').value;
            subSel.innerHTML = '<option value="">المادة</option>';
            
            // دمج المواد العامة مع مواد الشعبة
            const combined = [...subjectsData["عام"], ...(subjectsData[stream] || [])];
            [...new Set(combined)].forEach(s => subSel.innerHTML += `<option>${s}</option>`);
        }

        function showView(id) {
            document.querySelectorAll('.view-section').forEach(v => v.classList.remove('active'));
            document.getElementById(id).classList.add('active');
            if(window.innerWidth < 768) toggleSidebar();
        }

        function toggleSidebar() { document.getElementById('sidebar').classList.toggle('translate-x-full'); }
        function openModal() { document.getElementById('addModal').classList.remove('hidden'); }
        function closeModal() { document.getElementById('addModal').classList.add('hidden'); }

        new Chart(document.getElementById('pieChart'), {
            type: 'doughnut',
            data: { labels: ['إبتدائي', 'متوسط', 'ثانوي'], datasets: [{ data: [<%= stats.primary %>, <%= stats.middle %>, <%= stats.secondary %>], backgroundColor: ['#3b82f6', '#10b981', '#f59e0b'] }] },
            options: { cutout: '80%', plugins: { legend: { position: 'bottom', labels: { boxWidth: 10, font: { size: 10 } } } } }
        });

        async function updateAjax(id, type, index, btn) {
            let val;
            if(type === 'attendance') {
                val = !btn.classList.contains('active-dot');
                btn.classList.toggle('active-dot');
            } else {
                val = btn.innerText.includes('غير خالص');
                btn.innerText = val ? 'خالص' : 'غير خالص';
                btn.className = `text-[9px] px-2 py-1 rounded-md font-bold ${val ? 'bg-green-100 text-green-700' : 'bg-red-50 text-red-600'}`;
            }
            await fetch(`/update/${id}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ type, index, value: val.toString() }) });
        }
    </script>
</body>
</html>
